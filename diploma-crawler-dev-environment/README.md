# Diploma crawler development environment

Чтобы поднять БД в докере
```
docker compose up
```

Чтобы поднять БД в докере в detach режиме
```
docker compose up -d
```

Чтобы остановить БД в докере
```
docker compose stop
```

Чтобы удалить контейнеры
```
docker compose rm
```